import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion2.programacion.hibrida',
  appName: 'evaluacion2-programacion-hibrida',
  webDir: 'www'
};

export default config;
